---
/**
 * The address „Arhivă & Profil” used to answer at.
 *
 * The screen was split in three — the profile went to `/contul-meu`, the seats
 * request to the dashboard at `#locuri`, and the record of the year to
 * „Activitatea mea”, which has since been removed: what it reported is read
 * where it happens. The consultations held are on `/profesor/consultatii`, the
 * decided requests and their reasons on `/profesor/studenti`, and the session's
 * figures on the dashboard.
 *
 * This file cannot be deleted: the address is written in emails already
 * delivered (the seats decision carried it as its call to action) and in
 * whatever bookmarks people made of it. It now leads to the dashboard, which is
 * where every one of those threads starts.
 */
return Astro.redirect('/profesor', 301)
---
