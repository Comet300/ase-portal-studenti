---
/**
 * The address „Activitatea mea” answered at.
 *
 * The screen was a report on one coordinator's year: figures, the requests
 * received, the refusals and their reasons, the consultations held. It was
 * removed because every one of those is read better where it happens — the
 * consultations on `/profesor/consultatii`, the decided requests and their
 * reasons on `/profesor/studenti`, the session's figures on the dashboard — and
 * a fifth screen that only restated them was one more place to look.
 *
 * The address stays alive: it is in bookmarks, and `#locuri` still arrives here
 * from the seats emails, which the script below forwards on.
 */
return Astro.redirect(`/profesor${Astro.url.search}`, 301)
---
