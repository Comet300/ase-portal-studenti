{
  "name": "ase-portal-studenti",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "node scripts/start.mjs",
    "dev": "astro dev",
    "serve": "node ./dist/server/entry.mjs",
    "build": "astro check && astro build",
    "build:only": "astro build",
    "preview": "astro preview",
    "check": "astro check",
    "test": "node --experimental-strip-types --test test/*.test.ts",
    "verify": "npm run check && npm test",
    "db:migrate": "node scripts/migrate.mjs",
    "db:seed": "node scripts/seed.mjs",
    "db:reset": "docker compose down -v && npm start"
  },
  "dependencies": {
    "@astrojs/node": "^9.4.4",
    "astro": "^5.16.2",
    "pg": "^8.16.3",
    "resend": "^6.10.0"
  },
  "devDependencies": {
    "@astrojs/check": "^0.9.5",
    "@types/node": "^22.15.17",
    "@types/pg": "^8.15.5",
    "typescript": "^5.9.3"
  }
}
